const sum = (x, y) => x + y;
const product = (x, y) => x * y;
const difference = (x, y) => x - y;
const square = (x) => x * x;
const quotient = (x, y) => x / y;
const remainder = (x, y) => x % y;

module.exports = {
  sum,
  product,
  difference,
  square,
  quotient,
  remainder,
};
